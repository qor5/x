{
  "name": "front",
  "version": "1.0.0",
  "description": "tailwind",
  "main": "src/index.js",
  "scripts": {
    "build": "NODE_ENV=production npx tailwindcss --minify -i src/index.css -o ./public/index.css && node esbuild.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.12.9"
  }
}
